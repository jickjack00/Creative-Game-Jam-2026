using UnityEngine;

// this code aims to make a UI element FADE after a certain amount of time (3 seconds preferably, but we'll see what works)
// it stays fully 100% visible for 3 seconds before fading over a second or so (lerp can probably be used)

public class textFade : MonoBehaviour
{
    void Start()
    {
        
    }
}
